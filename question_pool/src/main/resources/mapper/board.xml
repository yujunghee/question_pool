<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board"> <!-- mapper파일의 이름 -->
	
	 <!-- 코드의 중복을 줄이기 위해 -->
	 <sql id="boardWhere">
	 	<where>
			<if test="searchType == '' ">
				(notice_title LIKE '%${searchWord}%' OR notice_content LIKE '%${searchWord}%')
			</if>
			<if test="searchType != null and searchType != '' ">
				${searchType} LIKE '%${searchWord}%'
			</if>
			<!--  
			<if test="admin_no > 0">
				AND admin_no = ${admin_no}
			</if>
			 -->
		</where>
	 </sql>
	 
	 <!-- 페이징처리 -->
	 <select id="count" resultType="int" parameterType="admin.board.NoticeVo">
	 	SELECT COUNT(*) FROM notice
	 	<include refid="boardWhere"></include>
	 </select>

	<select id="selectList" resultType="admin.board.NoticeVo" parameterType="admin.board.NoticeVo">
		SELECT * FROM notice
		<include refid="boardWhere"></include>
		ORDER BY notice_date DESC
		LIMIT ${startIdx}, 10
	</select>
	
	<insert id="insert" parameterType="admin.board.NoticeVo">
		<!-- #{}는 PreparedStatement, ${}는 Statement -->
		INSERT INTO notice (
			notice_title, notice_content, notice_date, admin_no, notice_readcount, notice_file_org, notice_file_real 
			) VALUES (
				#{notice_title}, #{notice_content}, NOW(), #{admin_no}, #{notice_readcount}, #{notice_file_org}, #{notice_file_real}
			) 
	</insert>
	
	<select id="selectOne" parameterType="int" resultType="admin.board.NoticeVo">
		SELECT * FROM notice WHERE notice_no = #{xxx}
	</select>
	
	<update id="updateReadcount" parameterType="admin.board.NoticeVo">
		UPDATE notice SET notice_readcount=notice_readcount+1 WHERE notice_no = #{notice_no}
	</update>
	
	<update id="update" parameterType="admin.board.NoticeVo">
		UPDATE notice SET 
			notice_title=#{notice_title} 
			, notice_content=#{notice_content}
			<if test="notice_file_org != null"> 
			, notice_file_org=#{notice_file_org} 
			, notice_file_real=#{notice_file_real}
			</if> 
		WHERE notice_no = #{notice_no}
	</update>
	
	<delete id="delete">
		DELETE FROM notice WHERE notice_no=#{notice_no}
	</delete> 
</mapper> 