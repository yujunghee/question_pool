<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userPay"> <!-- mapper파일의 이름 -->


	<select id="Payment" parameterType="userPayment.UserPayVo" resultType = "userPayment.UserPayVo">
		select * from user where user_no = 1
	</select>
	
	<!-- 	<insert id = "insert" parameterType="board.BoardVo">
		INSERT INTO board (
			pay_date, pay_way, user_no, admin_no, product_no
		) values(
			NOW(),#{pay_way},#{user_no},#{admin_no},#{product_no}
			)
	</insert>  -->

</mapper>